version: '3'

services:
  myapp:
    container_name: myapp
    restart: always
    build: .
    # ports:
    #   - '4301:4301'
    volumes:
      - ./myapp:/opt/myapp
    environment:
      - NODE_ENV=production
      - REDISCLOUD_URL=redis://rediscloud:omZlsfXvSumuVJI8CvM9dGfW4iBsVG9U@redis-10442.c10.us-east-1-4.ec2.cloud.redislabs.com:10442
    links:
      - redis
  redis:
    container_name: myapp-redis
    image: 'redis:4.0.11'
    ports:
      - '6379:6379'

#   traefik:
#     image: traefik:1.3.6-alpine
#     ports:
#       - "8081:8081"
#     volumes:
#     - ./traefik/traefik.toml:/traefik.toml
#     command: "--file"
#     networks:
#       - proxy

# networks:
#   proxy:
#     external: false
