setup:
  addons:
    - plan: rediscloud:30
      as: Redis-Cache
build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: development
release:
  command:
    - ./deployment-tasks.sh
  image: web
run:
  web: bundle exec puma -C config/puma.rb